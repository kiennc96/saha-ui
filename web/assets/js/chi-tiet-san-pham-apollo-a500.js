    document.querySelectorAll('.choice').forEach(btn=>btn.addEventListener('click',()=>{const group=btn.closest('[data-choice-group]');group.querySelectorAll('.choice').forEach(x=>x.classList.remove('active'));btn.classList.add('active');if(group.dataset.choiceGroup==='color')document.getElementById('selectedColor').textContent=btn.dataset.value}));
    function changeQty(delta){const input=document.getElementById('qty');input.value=Math.max(1,(parseInt(input.value)||1)+delta)}
    function addCart(){const qty=Math.max(1,parseInt(document.getElementById('qty').value)||1);document.querySelector('.bubble').textContent=qty;showToast(`Đã thêm ${qty} sản phẩm vào giỏ`)}
    document.querySelectorAll('.faq-q').forEach(btn=>btn.addEventListener('click',()=>btn.closest('.faq-item').classList.toggle('open')));
    const visual=document.getElementById('productVisual');const mainPhoto=document.getElementById('mainPhoto');
    document.querySelectorAll('.thumb').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('.thumb').forEach(x=>x.classList.remove('active'));btn.classList.add('active');const view=btn.dataset.view;if(view==='front')visual.innerHTML='<div class="tube"><div class="tube-label">APOLLO<small>A500</small><span>Neutral Silicone</span></div></div>';if(view==='side')visual.innerHTML='<div class="tube"><div class="tube-label" style="border-color:#64748b;color:#334155">THÔNG SỐ<span>300ml</span></div></div>';if(view==='box')visual.innerHTML='<div style="width:270px;height:190px;background:#c79b62;border:5px solid #9b6a35;border-radius:7px;display:grid;place-items:center;color:#fff;font-size:25px;font-weight:800;text-align:center">APOLLO A500<br><small>THÙNG 24 CHAI</small></div>';if(view==='application')visual.innerHTML='<img src="https://images.unsplash.com/photo-1505693416388-ac5ce068fe85?auto=format&fit=crop&w=900&q=80" alt="Ứng dụng nhôm kính" style="width:100%;height:510px;object-fit:cover">'}));
    function calculate(){const l=+document.getElementById('length').value||0,w=+document.getElementById('width').value||0,d=+document.getElementById('depth').value||0;const ml=l*w*d;const bottles=Math.max(1,Math.ceil(ml/300*1.1));document.getElementById('calcResult').textContent=bottles+' chai'}
    ['length','width','depth'].forEach(id=>document.getElementById(id).addEventListener('input',calculate));calculate();
    const anchors=[...document.querySelectorAll('.anchor-inner a')];const targets=anchors.map(a=>document.querySelector(a.getAttribute('href'))).filter(Boolean);
    addEventListener('scroll',()=>{let current=targets[0]?.id;targets.forEach(t=>{if(scrollY>=t.offsetTop-120)current=t.id});anchors.forEach(a=>a.classList.toggle('active',a.getAttribute('href')==='#'+current))},{passive:true});
